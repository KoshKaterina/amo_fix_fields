/.venv/
/.idea/
__pycache__/
.env
.env.test
tg_diagnostic.py

# ad-hoc / one-off probe scripts (investigation only, not part of the service)
# ⚠️ Здесь только поимённый список старых проб, БЕЗ маски amgroup_*.py.
# Маска молча прятала боевые модули протеза amgroup (fallback, lead_builder,
# shipment, duplicate_watch): 03.09.2026 выяснилось, что ни один из них ни разу
# не уехал в git. Поимённо - значит новый файл по умолчанию виден, а спрятать
# его можно только осознанно, дописав строку сюда.
amgroup_ff.py
amgroup_probe.py
amgroup_timeline.py
dp_probe.py
ms_demand_*.py
query_ur_egor*.py

# logs & build artifacts
*.log
__pycache__/

# runtime state/cache written by the service itself (lead_distribution rotation/counters,
# write-through cache of profiles from team-panel) — never source of truth, never commit
/var/
